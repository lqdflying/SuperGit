---
description: Managed refresh, coalescing, cache invalidation, and webview commit-sync invariants
globs: src/extension.ts,src/webview/App.tsx,src/git/commands.ts
alwaysApply: false
---

# SuperGit Refresh & Async Coordination

Lessons from fetch/prune/reload performance debugging. Read before changing `extension.ts`, `App.tsx` commit loading, or remotes cache.

## Managed refresh (`extension.ts`)

Any path that runs slow ref-mutating Git (`fetch --all --prune`, push, pull, prune-stale, title-bar Refresh) must:

1. Call `beginManagedRefresh(scope)` **before** the slow command (not after success).
2. Clear pending repo-watcher debounce timers on start.
3. Skip repo-change events **immediately** when guard is active (not only inside the 400ms callback).
4. Call `endManagedRefresh(generation)` only after fetch + reload finish; clear flag via generation-matched delayed settle.

**Never** use a shared boolean + unconditional `setTimeout(..., 1500)` to clear suppression — overlapping operations (e.g. prune-stale then Refresh) will clear each other's guards mid-fetch.

## In-flight coalescing

| Operation | Pattern |
|-----------|---------|
| `loadInitialData` | Dedupe in-flight promise; log `coalesced with in-flight load` |
| `enrichRemoteDefaultsInBackground` | Check inflight map **before** bumping generation; key = `root + sorted remote names` |
| Apply enriched remotes | Post only when `key === latestEnrichRemoteDefaultsKey` (handles A→B→A coalesce edge) |
| `loadBranchHistory` | Bump `branchHistoryLoadGeneration` on start; post data and clear `branch-history` loading only when `generation === branchHistoryLoadGeneration` (overlapping tab entry + post-action reload) |

Do **not** increment enrichment generation on coalesced return — that skips valid in-flight results.

## Cache invalidation

`invalidateRemoteDataCaches(root, { defaultBranches?: boolean })` — clears remotes list cache always; clears per-remote default-branch cache unless `defaultBranches: false`.

**Full invalidation** (`defaultBranches` default true) on:

- Successful manual refresh fetch
- Branch actions that may change remote default HEAD: `fetch`, `prune-stale`, `delete-remote`
- Repo-watcher debounced reload (external `git remote` add/remove, fetch/prune, remote HEAD changes)

**List-only invalidation** (`defaultBranches: false`) after safe branch actions (`push`, `pull`, `add-upstream`, `set-default-upstream`, `set-upstream`, `delete`) — remote names and default HEAD unchanged.

### Remote default enrichment gating

`loadRemotes(root, { enrichDefaults?: boolean })` — default `true`. Post-action `runBranchAction` passes `enrichDefaults: shouldEnrichRemoteDefaultsAfterAction(action)` (same set as full default-branch invalidation). When `false`, reuse `defaultBranch` from `lastRemotes` on the posted `remotes-data` — no background `ls-remote --symref`.

Log check after `add-upstream` on Tracking tab: `enrichRemoteDefaults start` **0**, `remote default branches enriched` **0**.

### Branch history cache

`src/extension/branchHistoryCache.ts` — key = `root + dateRange`. `loadBranchHistory` posts cache hit immediately (`branch history cache hit`); skips post when `activeWebviewTab !== "history"` (`branch history post skipped; tab inactive`).

**Invalidate on full reload:** `loadInitialDataInner` calls `markBranchHistoryDirty(root)` **before** the parallel `Promise.all` reload (bumps per-root cache epoch eagerly — even if a reload leg fails, stale cache is never served). After the reload succeeds, inline `loadBranchHistory` runs when History tab is active so Refresh on History posts fresh data.

In-flight history loads capture `getBranchHistoryCacheEpoch(root)` before `getBranchLifecycles()`; they discard results and skip `setBranchHistoryCache` when the epoch changes mid-flight (`branch history result discarded; cache epoch changed`).

After ref-changing **branch actions** when History tab inactive: `markBranchHistoryDirty(root)` only — no inline `getBranchLifecycles`. Reload on next History visit or when History tab active at action end.

### Commit graph dirty deferral

`commitGraphDirty` + `commitGraphDirtyEpoch` on extension host. After ref-changing actions when Graph tab inactive: skip `loadCommits`, set dirty + bump epoch, log `loadCommits deferred; graph tab inactive`. On `tab-changed` to `"graph"` while dirty → reload commits once. **`loadCommits` clears `commitGraphDirty` only when `commitGraphDirtyEpoch` is unchanged since the load started** — prevents an older in-flight request-commits from clearing a newer dirty mark set by a branch action during its execution.

Without dirty deferral, skipping post-action `loadCommits` would leave stale ref badges — webview does not request commits on Graph tab switch alone.

**Stale upstream cleanup:** `unsetStaleUpstreamLinks()` runs only from explicit **Prune Stale** (confirmed action), not title-bar Refresh. Abort cleanup when `refs/remotes/` for-each-ref fails — never treat a failed listing as “no refs exist.”

**Post-action reload:** `fetch` and `prune-stale` must eventually refresh commits (inline on Graph tab, or via `commitGraphDirty` on return). `delete-remote` must reload branches, remotes, and commits.

**Branch history after actions:** Gate inline reload with `shouldReloadBranchHistoryAfterAction(actionTab, currentTab)`; otherwise `shouldMarkBranchHistoryDirty(action)` when applicable. Classifiers live in `src/extension/refreshPolicy.ts`.

## Webview commit requests (`App.tsx`)

Initial `ready` → host `loadInitialData` already loads commits. Avoid duplicate `request-commits`.

**Do not** use one-shot skip flags cleared inside a `useEffect` — if deps unchanged (e.g. `pagination.page` stays 0), the flag is never consumed and the **first real user change is swallowed**.

**Use query-key matching:**

- `satisfiedCommitQueryKey` = JSON of `{ dateRange, page, searchText, scope }` last satisfied by host
- Update on every `commits-data` (use refs for current filters in the message listener)
- Post `request-commits` only when current query key ≠ `satisfiedCommitQueryKey`

## Log verification (count, do not vibe-check)

Before claiming a refresh fix, grep the latest `supergit.log`:

| Invariant | One manual Refresh |
|-----------|-------------------|
| `managed refresh start` | 1 |
| `fetch --all --prune` | 1 start, 1 complete |
| `loadInitialData start` | 1 after fetch (not during) |
| `refreshing after repository change` | 0 mid-fetch |
| `repository change skipped; managed refresh already running` | may be >0 during fetch |

After `add-upstream` on **Branch Tracking** tab:

| Invariant | Expected |
|-----------|----------|
| `enrichRemoteDefaults start` | 0 |
| `remote default branches enriched` | 0 |
| `loadCommits deferred; graph tab inactive` | 1 (if Graph inactive) |
| `branch history marked dirty` | 1 (if History inactive) |

History tab round-trip (same date range, no intervening ref action):

| Invariant | Expected |
|-----------|----------|
| `branch history cache hit` | 1 on second visit |
| `loadBranchHistory start` | 0 on cache hit |

Also test **overlap**: Prune Stale immediately followed by title-bar Refresh.

## Static review triggers

Re-read diffs that introduce:

- `setTimeout` clearing shared suppression/coalescing state
- Message handler sets a ref flag; effect clears it (verify effect always runs when flag is set)
- Global single promise for enrichment without per-key + latest-key apply guard

Network-bound `fetch --all --prune` (~5s) is expected; the win is eliminating **extra** full reloads during fetch. Refresh button: disabled + tooltip while `loading` scope `all`.
